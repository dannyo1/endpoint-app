import React from "react";

const Footer = () => {
  return (
    <div className="footer">
      <footer>© {new Date().getFullYear()} - danny :-) </footer>
    </div>
  );
};

export default Footer;
